<%= form_for(@mark) do |f| %>
  <% if @mark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mark.errors.count, "error") %> occurred.</h2>
      <ul> 
        <% @mark.errors.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :url %>
    <%= f.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :tag_list, 'Tags' %> <small>(comma seperated)</small><br />
    <%= form.text_field :tag_list, :value => @mark.tags_listing %>
  </div>

  <div class="actions">
    <%= f.submit "Add Zipmark" %>
  </div>
<% end %>