<tr>
  <td><%= link_to mark.title, mark.url %></td>
  <td><%= mark.tag %></td>
  <td>
    <% if mark.user == current_user %>
      <%= link_to "Delete?", mark, :method => :delete, :prompt => "Are you sure?", :class => 'btn btn-danger btn-mini' %>
    <% end %>
  </td>
</tr>