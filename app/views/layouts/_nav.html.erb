<div class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>

      <%= link_to "Zipmark", root_path, :class => "brand" %>

      <!-- Nav Links -->
      <div class="nav-collapse collapse">
        <!-- hide everything here -->
        <ul class="nav pull-right">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= user_signed_in? ? current_user.username : "Guest" %> <b class="caret"></b></a>
              <ul class="dropdown-menu">
                <% if user_signed_in? %>
                  <li><%= link_to "Logoff", destroy_user_session_path, :method => :delete %></li>
                <% else %>
                  <li><%= link_to "Create account", new_user_registration_path %></li>
                  <li><%= link_to "Sign in", new_user_session_path %></li>
                <% end %>
              </ul>
          </li>
        </ul>
      </div>
      <!-- /navlinks -->

    </div>
  </div>
</div>